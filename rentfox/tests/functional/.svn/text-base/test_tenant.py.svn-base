from rentfox.tests import *

class TestTenantController(TestController):

    def test_index(self):
        response = self.app.get(url(controller='tenant', action='index'))
        # Test response...
