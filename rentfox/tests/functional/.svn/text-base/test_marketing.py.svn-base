from rentfox.tests import *

class TestMarketingController(TestController):

    def test_index(self):
        response = self.app.get(url(controller='marketing', action='index'))
        # Test response...
