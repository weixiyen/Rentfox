from rentfox.tests import *

class TestTenantUnitController(TestController):

    def test_index(self):
        response = self.app.get(url(controller='tenant_unit', action='index'))
        # Test response...
