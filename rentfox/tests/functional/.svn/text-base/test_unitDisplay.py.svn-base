from rentfox.tests import *

class TestUnitdisplayController(TestController):

    def test_index(self):
        response = self.app.get(url(controller='unitDisplay', action='index'))
        # Test response...
